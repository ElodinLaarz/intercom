root = true

[*.{kt,kts}]
# Jetpack Compose @Composable functions are PascalCase by convention; don't let
# the function-naming rule fight that.
ktlint_function_naming_ignore_when_annotated_with = Composable
