# STATUS

Per V2_PLAN.md rule 2: status is what ran on hardware, not what compiles.
Update at every milestone gate and any on-device session.

**Current milestone: M1 — tracer bullet (next).** M0 (infrastructure) closed 2026-06-14 — gate passed on both phones.

| Subsystem | Last validated on device | Device(s) | What passed |
|---|---|---|---|
| App shell (stub) | 2026-06-14 | Pixel 10 Pro XL + moto g play 2024 (wireless adb) | `tools/smoke`: install + launch + `STARTED version=0.0.1` + `NATIVE selfTest=0xC0DE` on BOTH phones |
| CI (Forgejo) | 2026-06-14 | `flutter` runner (Ubuntu 24.04 container) | NDK 27.2.12479018 + cmake 3.22.1 installed in-CI via sdkmanager; preflight (bootstrap) green; host `ctest` 2/2; NDK-backed `assembleDebug` BUILD SUCCESSFUL (run 7, PR #9). PR-head-sha checkout verified |
| Native core (ring/seq) | 2026-06-14 | CI host (Ubuntu) | host `ctest` 2/2: `RingBuffer` drop-oldest + overwrite count, `SeqFilter` epoch gate + 2^32-wrap. On-device `NativeCore.selfTest`=0xC0DE verified at the M0 phone gate |
| Smoke harness | 2026-06-14 | Pixel 10 Pro XL + moto g play 2024 | `tools/smoke` PASS on both (the M0 gate). Caught + fixed a CRLF parse bug on the Windows host |
| Radio (BLE link) | 2026-06-15 | Pixel 10 Pro XL (guest) + moto g play 2024 (host) | M1.5 (#20): **first L2CAP CoC voice frame.** moto host PSM 229; Pixel guest scanned, read PSM 229, opened CoC, sent `VoiceFrame(epoch=1, seq=0)`; host accepted and decoded first frame; host Stop closed CoC and guest detected host loss/rescan. |
| Voice pipeline | — | — | M1, not started |

## Log

- 2026-06-12 — repo created; skeleton scaffolded (Compose stub, `INTERCOM: STARTED` log line).
- 2026-06-12 — first build (59 s, Gradle 8.14/AGP 8.11.1) + first device run: installed and launched on Pixel 10 Pro XL over USB, STARTED log confirmed. Screen was locked (owner on a call) so no UI screenshot; log line is the validation.
- 2026-06-14 — codec decision Opus→ADPCM recorded (PR #8). CI taken native: in-CI sdkmanager installs NDK r27c + cmake 3.22.1, fail-loud bootstrap preflight, host `ctest` (ring_buffer, seq_filter), NDK-backed `assembleDebug` — all green (PR #9, run 7). Riskiest M0 infra item (runner native toolchain) proven. Phones not yet paired; M0 gate still open.
- 2026-06-14 — ktlint + detekt gates added (PR #13). **M0 gate PASSED:** both phones paired over wireless adb; `tools/smoke` installed the stub and asserted `STARTED version=0.0.1` + `NATIVE selfTest=0xC0DE` on Pixel 10 Pro XL and moto g play 2024. The gate immediately earned its keep — caught a CRLF device-parse bug in the harness on the Windows rig host. **M0 (infrastructure) closed.** Next: M1 tracer bullet.
- 2026-06-15 — **M1.5 (#20) L2CAP CoC + first voice frame (rig-verified).** moto host tapped Host → `RADIO l2cap listening psm=229`, `advertise onStartSuccess`; Pixel guest tapped Guest → `scan match`, `gatt read PSM=229`, `l2cap tx frame epoch=1 seq=0`; host logged `l2cap accepted` and `l2cap rx frame epoch=1 seq=0 (first)`. Host Stop then produced guest `l2cap host closed — link lost` + `Host disconnected`, proving teardown over the CoC path.
- 2026-06-15 — **M1.4 teardown bugfix (rig-verified, #19/#31).** Two link-teardown bugs found on the two-phone rig: (1) host Stop left the guest stuck on "Linked" — a GATT server can't drop a *client-initiated* connection with `cancelConnection` alone; fixed with adopt-then-cancel (`connect(device, false)` then `cancelConnection`), guest now shows `Disconnected (status 19)` in ~2 s; (2) after guest Stop → rehost → rejoin, a buffered DISCONNECT from the torn-down guest clobbered the new session ("Disconnected" on rejoin) — fixed with a `torn` guard that mutes a stopped instance's callbacks (rule 4 — no stale state across sessions). Both verified on the rig (moto host + Pixel guest): Stop → rehost → rejoin now lands clean on `Linked — host PSM 188`.
- 2026-06-15 — **M1.4 (#19) first two-phone link.** moto hosting (advertise + GATT, PSM 182); Pixel guest tapped Join → `RADIO scan match` (MSD filter; the host's RPA had rotated and the filter still hit — landmine #2 working) → `gatt conn newState=2` → `mtu=517` → `read PSM=182`, matching the host. Host side confirmed `gatt read psm by <guest>`. Single clean tap leaves the guest a guest (no role-flip / auto-reconnect — reconnect is M2); the earlier flapping was repeated-tap test noise. Pixel restored to its prior app afterward. Next: #20 (L2CAP CoC open + test frame).
- 2026-06-15 — **M1.3 (#18) host radio — first on-device radio validation.** On the moto (landmine #13 canary): tapped Host → `RADIO l2cap listening psm=177`, `RADIO gatt serviceAdded status=0`, `RADIO advertise onStartSuccess txPower=3`. Minimal MSD-only adv (no 128-bit UUID, `mAdvertisingServiceUuids` empty) — **no payload overflow on the OEM-quirk device**. Insecure L2CAP listener publishes a real PSM (owner decision, this PR). Pixel left untouched (owner using it). Guest scan/connect + the actual cross-device PSM read are #19–20. M1.2 codec (#17) merged earlier today (host ctest 4/4 + Kotlin tests green on CI; host-only, no device row).
- 2026-06-14 — **M1.1 design sign-off (issue #16):** §2 voice-path params locked with the owner — **16 kHz** wideband, jitter depth **3 frames (~60 ms, rig-tuned)**, **silence-fill** PLC, **Kotlin owns the L2CAP socket**. Codec contract pinned (pre-roll IMA: continuous predictor snapshotted per frame; 320 PCM samples/frame; `predSample` not emitted). Design-gate cleared — voice code unblocked. Next: #17 (proto voice frame + codec, host-only). *(Doc decision, not an on-device validation — the table above stays empty for radio/voice until the rig says otherwise.)*
